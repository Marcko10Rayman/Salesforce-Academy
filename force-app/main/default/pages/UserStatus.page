<apex:page >
    
    <apex:pageBlock title="User Status">
        <apex:pageBlockSection columns="1">
        	<b><p>
                {! $User.FirstName } {! $User.LastName }		<!--show different user's elements-->
           		({! $User.Username }) ({! $User.Email })

            </p></b>
        		
            <p>{! $User.FirstName &  ' ' & $User.LastName }</p> <!--contactenated the user's name and the lastname-->
            
            <p> Today's Date is {! TODAY() } </p>				<!--Show the date-->
           <p> Next week it will be {! TODAY() + 7 } </p>		<!--show the following day in 7 days more.-->
            
            
            <p>The year today is {! YEAR(TODAY()) }</p>
			<p>Tomorrow will be day number  {! DAY(TODAY() + 1) }</p>
			<p>Let's find a maximum: {! MAX(1,2,3,4,5,6,5,4,3,2,1) } </p>
			<p>The square root of 49 is {! SQRT(49) }</p>
            <p>Is it true?  {! CONTAINS('salesforce.com', 'force.com') }</p> <!--!It returns a Boolean value: something that is either true or false. It compares two arguments of text and returns true if the first argument contains the second argument. If not, it returns false. In this case, the string “force.com” is contained within the string “salesforce.com”, so it returns true.-->
            
            <p>{! IF( CONTAINS('salesforce.com','force.com'), 
     			'Yep', 'Nope') }</p>
			<p>{! IF( DAY(TODAY()) < 15, 
     			'Before the 15th', 'The 15th or after') }</p>
            
            ({! IF($User.isActive, $User.Username, 'inactive') })
            
        </apex:pageBlockSection>
    </apex:pageBlock>
    
</apex:page>